// server/routes/controllers.js
// compiles the various backend routes into a single file

module.exports = (app, keys) => {
  console.log("app".unicorn, app);
  console.log("keys".unicorn, keys);
};
